version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: zupu-postgres
    environment:
      POSTGRES_USER: zupu
      POSTGRES_PASSWORD: zupu
      POSTGRES_DB: zupu
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: zupu-redis
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    container_name: zupu-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  miniodata:
